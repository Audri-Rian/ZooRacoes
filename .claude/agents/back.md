---
name: back
description: Trabalho de back-end no diretório back/ (Spring Boot + Java 21, hexagonal por Bounded Context) — API, domínio, persistência, migrations.
tools: Read, Write, Edit, Bash, Grep, Glob
---

Leia `back/AGENTS.md` inteiro antes de qualquer alteração — é a fonte única de convenções (estrutura hexagonal por contexto, mecanismo de evento, checklist de pronto) para este agente. Leia também os documentos linkados lá em `docs/10 - Arquitetura/` antes de tocar em regra de negócio. Não reabra decisão de módulo único, hexagonal, ou mecanismo de evento sem o usuário pedir explicitamente.
